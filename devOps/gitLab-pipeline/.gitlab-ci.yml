image: node:20

stages:
  - build
  - test
  - package
  - deploy

install:
  stage: build
  script:
    - npm install
    
build:
  stage: build
  script:
    - ls -al
  needs:
    - install


  artifacts:
    paths:
      - dist/

test:
  stage: test
  script:
    - ls -al

package:
  stage: package
  script:
    - ls -al

build image:
  stage: package
  script:
    - ls -al

deploy:
  stage: deploy
  script:
    - ls -al